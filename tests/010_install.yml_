---

- name: Install
  hosts: all
  pre_tasks:
    - include: pre_tasks/docker.yml
    - include: pre_tasks/backports.yml
      when: ansible_distribution_release == 'wheezy'
  roles:
    - manala.docker
